# 0.2026266.2326 — fix(gh): install the org tag-on-merge dispatcher + sweep stale cache prose

## Summary

Close the gaps left by the merged `document`-op cutover (#4): plugin-gh landed **untagged and therefore unconsumable**, and its README still described the pre-ArtifactStore cache.

- **`.github/workflows/tag-on-merge.yml`** (new): install the org-wide tag-on-merge dispatcher, copied from `opencharly/.github`'s canonical source (`dotgithub/org-wide-tag-on-merge-dispatcher.yml`, installed verbatim into every repo). plugin-gh shipped **only** `ci.yml`, so GitHub never minted a CalVer tag for any merge — the `document` op merged at `be6c6ff` with **no tag a proxy consumer can name**. Every sibling plugin repo already carries this dispatcher; plugin-gh was the outlier.
- **`README.md`**: sweep the pre-ArtifactStore prose — the shared response cache is the `spec/cache` **ArtifactStore** (an OCI Image Layout), not the old keyed `Store`.
- **`candy/plugin-gh/input_schema_test.go`** (new): pin the `pr` → `number` hard cutover — the legacy `pr` field is rejected at the verb input.

### Cross-repo landing order (BLOCKING)

The dispatcher calls `opencharly/.github/.github/workflows/tag-on-merge.yml@main` **at run time**. That reusable hardcoded `goModuleRepos = ['sdk','spec']` and would mint a plain `v<YYYY>.<DDD>.<HHMM>` tag for plugin-gh — **unusable as a module version** for its consumers, which pin `v0.<YYYYDDD>.<HHMM>` (the exact rot that once made `spec` unconsumable). **opencharly/.github#113** (now MERGED) adds `plugin-gh` to that set. With it + this dispatcher, plugin-gh tags every future merge as `v0.<YYYYDDD>.<HHMM>` + writes `CHANGELOG/0.<YYYYDDD>.<HHMM>.md`.

The already-merged head (`be6c6ff`, 2026-09-23 19:03:40Z) needs an **add-only backfill** tag `v0.2026266.1903` (tags are immutable; a backfill only adds one) — done from the owning session now that #113 is on `main`.

## How tested

**1. The dispatcher is byte-identical to the org's canonical source** (real `sha256sum`, both files):

```
$ sha256sum .github/workflows/tag-on-merge.yml
a102b20c5bf7bc2246710c6eb34a66c7421cbb371b4b237d8469ecae1d6c812f  .github/workflows/tag-on-merge.yml
$ sha256sum dotgithub/org-wide-tag-on-merge-dispatcher.yml   # in the umbrella checkout
a102b20c5bf7bc2246710c6eb34a66c7421cbb371b4b237d8469ecae1d6c812f  dotgithub/org-wide-tag-on-merge-dispatcher.yml

$ diff .github/workflows/tag-on-merge.yml dotgithub/org-wide-tag-on-merge-dispatcher.yml && echo "(no diff)"
(no diff)
```

The canonical source's own header says "Copy VERBATIM into every repo" and itself carries the `PR #3`/`PR #7`/`PR #8`/`8 merges and 17 schedule runs` RCA prose (it is the org's shared template, not plugin-gh-specific):

```
$ grep -c "PR #3's tag landed\|PR #7's tag was still missing\|8 merges and 17 schedule runs" dotgithub/org-wide-tag-on-merge-dispatcher.yml
3
```

**2. YAML validity:**

```
$ yq -e '.' .github/workflows/tag-on-merge.yml >/dev/null && echo "YAML OK"
YAML OK
```

**3. The `pr`→`number` hard cutover, proven against the plugin's OWN embedded schema** (the same `#GhInput` the host splices and validates authored input against):

```
$ go test -count=1 -run TestGhInputRejectsLegacyPr -v .
=== RUN   TestGhInputRejectsLegacyPr
--- PASS: TestGhInputRejectsLegacyPr (0.00s)
PASS
ok  	github.com/opencharly/plugin-gh/candy/plugin-gh	0.020s
```

The test asserts a valid `{op,repo,number}` input passes and a legacy `{…,"pr":2}` input is rejected naming `pr`. (A CUE struct definition is closed by default, so the served `#GhInput` already rejects the unknown field; verified independently that `#GhInput: {…}` and `#GhInput: close({…})` both reject `pr` — `close()` is redundant and is NOT added.)

**4. Repo gates, real unedited output:**

```
$ gofmt -l .
(no output — exit 0)
$ go build ./...
(no output — exit 0)
$ go vet ./...
(no output — exit 0)
$ go test -count=1 ./...
ok  	github.com/opencharly/plugin-gh/candy/plugin-gh	0.020s
?   	github.com/opencharly/plugin-gh/candy/plugin-gh/cmd/serve	[no test files]
ok  	github.com/opencharly/plugin-gh/candy/plugin-gh/gh	0.290s
?   	github.com/opencharly/plugin-gh/candy/plugin-gh/params	[no test files]
exit=0
```

**5. R5 sweep — the stale wording is gone, real command + output:**

```
$ grep -rnE "spec/cache\` Store|charly cache Store|cache\.Store|repo: \.\.\., pr:" --include=*.go --include=*.yml --include=*.md . | grep -v CHANGELOG/
(no output — exit 1)
$ grep -rnE "\bpr:" --include=*.go --include=*.yml --include=*.md . | grep -v CHANGELOG/
(no output — exit 1)
```

Only historical `CHANGELOG/2026.252.1500.md` mentions the retired `pr:` form (permitted historical context).

**6. The core op still works live** (unchanged by this PR; re-confirmed on the merged head): `gh document --repo opencharly/plugin-gh --number 4 --format json` → kind=pr, 14 commits, 16 files with full content, 3 comments; warm read `provenance.cached=true`.

## Rulebook compliance

- **R0** — loaded `/charly-internals:git-workflow`, `/charly-internals:plugin`, `/charly-build:docs`.
- **R1** — the missing tag and the stale cache prose are real divergences; the "close() needed" hypothesis was tested and **falsified** (CUE defs close by default), so it is not shipped.
- **R2** — the whole gap set for this repo is fixed here; the org-side tag-form fix landed as opencharly/.github#113 with the landing order enforced.
- **R3** — the dispatcher is the ONE org canonical copy (byte-identical, no local variant); the cache wording reuses the ArtifactStore name.
- **R4** — no workaround.
- **R5** — the stale `Store` wording is replaced; the sweep is pasted above.
- **R6** — `fix/gh-doc-gaps` off `origin/main`; no force-push.
- **R7 / R7a** — the closed-input contract has a fail-without-fix test (executed); the workflow dispatcher has no pre-merge execution surface (GitHub Actions only runs it on a merge to `main`, and no local workflow runner is installed), so its correctness is established by byte-identity to the canonical dispatcher already running live in every sibling org repo + a YAML parse.
- **R8 / R9** — CI workflow + docs + test only; no label/runtime dependency change.
- **R10** — hook/workflow class: byte-identity to the live-running org canonical + YAML parse + the executed repo-side contract test.

## Change classification

- **Change class:** Hook / workflow config + docs + a repo-side Go contract test.
- **Verification gate:** byte-identity to the org canonical dispatcher (real hashes) + YAML parse; the `#GhInput` contract test (executed); `gofmt`/`go build`/`go vet`/`go test` (real output pasted).
- **Attribution tier:** `analysed on a live system` — the workflow dispatcher's changed path has no pre-merge execution surface, so it is not `fully tested`; its correctness rests on byte-identity to the canonical dispatcher running live in every sibling org repo, plus the executed contract test and YAML parse.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*